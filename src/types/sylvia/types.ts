// Navbar 

export type NavItem = {
<<<<<<< Updated upstream
  label: string;
  to: string;
};

export interface NavbarProps {
  links?: NavItem[];
  user?: { name?: string; avatarUrl?: string } | null;
  onSearch?: (query: string) => void;
}



// checkouts 



=======
    label: string;
    to: string;
};

export interface NavbarProps {
    links?: NavItem[];
    user?: { name?: string; avatarUrl?: string } | null;
    onSearch?: (query: string) => void;
}

  // checkouts 
>>>>>>> Stashed changes
